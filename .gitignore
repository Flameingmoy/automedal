# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Competition data — all per-competition files live under data/ and are
# untracked (too large, each user pulls their own via scout/bootstrap).
# This covers raw CSVs (train/test/sample_submission), preprocessed .npy
# arrays, metadata JSON, and generated submission CSVs.
data/*.csv
data/*.npy
data/*.json
playground-series-*/

# Experiment logs & results
results.tsv
agent_loop.log

# Scout outputs (generated, not committed)
scout/outputs/

# Agent working files
worktrees/
results/
queue/
dev/
catboost_info/
