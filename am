#!/usr/bin/env bash
# am — AutoMedal convenience wrapper (thin shim)
# Delegates to `python -m automedal` which is the real entry point.
# For dev work in this repo, `uv run` ensures the right venv is used.
set -euo pipefail
cd "$(dirname "$0")"
exec uv run python -m automedal "$@"
